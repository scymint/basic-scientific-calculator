A basic scientific calculator made using only python and the math module.

It can handle all types of output however it will crash if a function returns undefined!

By default, it is in radians mode and cannot be changed so you must manually change the degrees before inputting it into the program.

Made by Dyllan W.
